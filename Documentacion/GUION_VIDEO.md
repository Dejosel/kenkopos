# Guion del Video de Demostración
## Evidencia: GA7-220501096-AA5-EV04 – API del proyecto (KenkoPOS)

---

| Tiempo | Sección | Acción en Pantalla / Lo que se muestra | Guion Hablado (Voz en off) |
|---|---|---|---|
| **0:00 – 0:30** | **Presentación Inicial** | Mostrar la pantalla del proyecto **KenkoPOS** en VS Code o en el navegador. | *"Un cordial saludo, mi nombre es **Jose Luis Hernandez**, aprendiz del programa Tecnólogo en Análisis y Desarrollo de Software (ADSO) del SENA. En esta ocasión voy a presentar la evidencia **GA7-220501096-AA5-EV04: API del proyecto**, basada en nuestro proyecto real **KenkoPOS**, un sistema de Punto de Venta desarrollado en PHP 8 con arquitectura MVC."* |
| **0:30 – 1:00** | **Repositorio y Estructura** | Abrir la página del repositorio en GitHub (`https://github.com/Dejosel/kenkopos`) y mostrar la estructura de archivos en el editor (`api/products.php`, `ENDPOINTS.md`). | *"Como observamos en pantalla, nos encontramos en el repositorio oficial en GitHub en su versión v1.2.0. El proyecto cuenta con una arquitectura modular. Para esta evidencia nos centraremos exclusivamente en el módulo de **Productos**, el cual permite gestionar el catálogo de nuestro POS. Cabe resaltar que la autenticación de usuarios ya fue demostrada en una evidencia previa."* |
| **1:00 – 1:40** | **Prueba 1: GET Todos los Productos** | Abrir Postman, seleccionar la colección `KenkoPOS API - Productos`, abrir la petición `1. Obtener Todos los Productos` y hacer clic en **Send**. Mostrar la respuesta JSON y los Test Results. | *"Ingresamos a Postman a nuestra colección 'KenkoPOS API - Productos'. Primero ejecutamos la petición GET a `/api/products.php` usando la variable de entorno `base_url`. Vemos que nos retorna el código HTTP 200 OK en menos de 10 ms, con una respuesta JSON que contiene el listado completo de productos en la propiedad `products`. Todos los 9 tests automáticos pasan exitosamente."* |
| **1:40 – 2:20** | **Prueba 2: GET Producto por ID** | Seleccionar `2. Obtener Producto por ID` en Postman (`/api/products.php?id=1`) y enviar la petición. | *"Continuando con el CRUD, probamos la lectura individual vía GET enviando el parámetro `id=1` en la URL. El servidor nos responde con HTTP 200 OK y nos entrega la información detallada de la 'Hamburguesa Especial Premium' en la propiedad `product`."* |
| **2:20 – 3:00** | **Prueba 3: POST Crear Producto** | Seleccionar `3. Crear Producto`, mostrar el JSON del Body (`Pizza Personal`, `PZ001`, `22000`, `Comidas`, `Rojo`) y enviar. | *"Ahora probamos la creación de un nuevo producto usando el método POST. En el cuerpo JSON enviamos el nombre 'Pizza Personal', el SKU 'PZ001', precio 22.000 y categoría 'Comidas'. Al enviar la petición, obtenemos una respuesta HTTP 201 Created con el mensaje 'Producto creado correctamente'."* |
| **3:00 – 3:45** | **Prueba 4: PUT Actualizar Producto** | Seleccionar `4. Actualizar Producto`, mostrar el Body JSON de actualización (`product_id: 1`, `Pizza Especial`, precio `25000`) y enviar. | *"En la cuarta prueba validamos la actualización con el método PUT. Enviamos en el cuerpo JSON el `product_id` a modificar con los nuevos datos, como el nombre 'Pizza Especial' y el precio actualizado a 25.000. El servidor nos responde con HTTP 200 OK y confirma que el producto fue actualizado exitosamente."* |
| **3:45 – 4:30** | **Prueba 5: DELETE Eliminar Producto** | Seleccionar `5. Eliminar Producto` (`/api/products.php?id=15`) y enviar la petición. | *"Finalmente, probamos la eliminación mediante el método DELETE especificando el ID del producto que acabamos de crear. Recibimos un código HTTP 200 OK con el mensaje 'Producto eliminado correctamente'. Adicionalmente, si volvemos a intentar eliminar el mismo ID, la API nos responde con un código 404 Not Found, demostrando el correcto manejo de errores."* |
| **4:30 – 5:00** | **Persistencia, Repositorio y Cierre** | Mostrar brevemente la base de datos (SQLite DB Browser o archivo `kenkopos.sqlite`), el archivo `ENDPOINTS.md` en GitHub/VS Code y cerrar. | *"Podemos verificar que todas las operaciones se reflejaron correctamente en nuestra base de datos. Todos los cambios, junto con el archivo `ENDPOINTS.md` y la colección de Postman, se encuentran actualizados en nuestro repositorio de GitHub. Con esto concluimos satisfactoriamente la prueba completa de la API del módulo de Productos para la evidencia EV04. Muchas gracias por su atención."* |

---
